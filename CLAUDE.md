# 最優先ルール（例外なし）

## 終わっていない作業があるなら、次の一手を必ず AskUserQuestion（ダイアログ）で出す

**判定の基準は「選択肢を書いたかどうか」ではない。「まだ終わっていない作業があるかどうか」。**

未完了の作業が 1 件でも残った状態でターンを終えるなら、**必ず AskUserQuestion を使う**。
地の文・箇条書き・表で選択肢を並べて「どれにしますか？」と聞くのは**禁止**。

これは最優先ルールであり、**これを破ってよい例外は一切ない**。
急いでいても、選択肢が2つでも、話の流れ上自然に見えても、例外にしない。

### 対象になる場面（すべてダイアログ）

- **次のステップ・残件・未完了の作業を提示するとき**（疑問符が無くても対象）
  「残っているのは A と B と C です」と本文に並べて相手に選ばせるのは違反

- **他者・外部イベント待ちで止まるとき**
  待つこと自体が選択であり、待つ以外の道（別手段・先に別作業・中止）を選べる状態にする

- 「A / B どちらにしますか」と尋ねるとき
- 「進め方の選択肢は次の3つです」と並べるとき
- 「〜する？それとも〜する？」と二択を投げるとき
- 表やリストで案を比較して選ばせようとしているとき
- 「どれで進めますか？」「どうしますか？」で終わる文を書こうとしたとき

### 判定方法

**自分の返答を送信する前に確認する**:
「この返答は、ユーザーに何かを選ばせようとしているか？」
→ **YES なら、文章で選択肢を書かず AskUserQuestion を呼ぶ。**

選択肢を文章で書き始めたら、その時点で手を止めてツール呼び出しに切り替える。

### 例外にならないもの（よくある言い訳）

- 「ダイアログが流れた/答えてもらえなかった」→ 文章で並べ直すのではなく、必要なら質問を作り直して**もう一度ダイアログで出す**
- 「選択肢が明らかだから」→ それでもダイアログ
- 「補足として書いただけ」→ 選ばせているならダイアログ
- 「技術的な比較表だから」→ 選択を促すならダイアログ

### 言い換えて回避するのは、選択肢を本文に並べるより悪い

フックに差し戻されたとき、**検知語を避けて書き直すのは違反である。**
差し戻しは「書き方を直せ」ではなく「ダイアログを出せ」という指示。

実際に起きた回避の例（すべて違反）。

- 「残件」「次のステップ」を消して「〜まで進みません」と言い換えた
- 「どうしますか？」を消して「連絡をもらえた時点で再開します」と言い換えた
- 判断をユーザーに委ねたまま、疑問符だけ削って言い切りの形にした

いずれも**残作業がある状態でダイアログを出していない**点で同じ違反。
差し戻されたら、文面ではなく AskUserQuestion を出すことで応じる。

### 例外にしてよい唯一のケース

**選択を求めていない**とき。つまり:
- 単に事実・状態・結果を報告している
- すでにユーザーが決めた方針を実行している
- 参考情報として比較を示すだけで、次の行動を選ばせていない

判断に迷ったら**ダイアログを使う**。使いすぎて困ることはない。

## API 課金が発生する操作は必ず事前確認

**Console 経由で費用が発生しうる操作は、いかなる作業であっても着手前にユーザーへ確認する。**
リサーチ・検証・動作確認・「ちょっと試すだけ」も対象。

**これは禁止ではない。** 費用がかかること自体は問題ない。防ぎたいのは
**ユーザーが知らないところ、認識が甘いところで費用が発生すること**。順序を守る。

1. **先に見積もる。** 使うモデル・想定トークン数・単価から概算を出す。
2. **`AskUserQuestion` で提示する。** 何のために、いくらかかる見込みかを示して許可を得る。
3. **許可を得てから実行する。** 見積もりを大きく超えそうなら、その時点で再度確認する。

### このリポジトリ固有の注意

**`DRY_RUN=true` は無料ではない。** `scripts/generate-recs.mjs` は
`if (DRY_RUN)` の判定より**前**に Anthropic API を呼ぶ。DRY RUN がスキップするのは
Firebase への書き込みだけで、**API 課金は発生する**。

- 1回あたりの目安: **$0.4〜$1.0**（Sonnet 4 + `web_search` を最大8回使用）
- 内訳: web検索 8回で $0.08、入力は検索結果の積み上がりで 10万〜30万トークン規模
- 日次スケジュールを有効化すると **月 $12〜$30 程度**

`.github/workflows/daily-recs.yml` の `schedule:` は現在コメントアウトされており、
自動実行はされていない。**有効化は費用の見積もりを提示して承認を得てから行う。**

**このルールもフックで強制している。** `.claude/hooks/guard-api-cost.py` が該当コマンドを
ブロックする。仕組みは [`docs/api-cost-guard.md`](docs/api-cost-guard.md) を参照。

---

# このリポジトリについて

BubblesNow（個人向けタスク管理PWA）を Netlify + Make.com から
GitHub Pages + GitHub Actions へ移管するプロジェクト。

- 現状の機能・データ構造の棚卸し: `docs/CURRENT_SPEC.md`
- 運用手順と環境の制約: `docs/OPERATIONS.md`
- 移管の全体計画: `BubblesNow_GitHub移管_実装設計書_v2.md`

## 環境上の重要な制約

クラウドの Claude Code セッションは egress ポリシーにより **Firebase・github.io・
一般サイトに到達できない**（CONNECT が 403）。到達できるのは GitHub API と Web検索のみ。

そのため **Firebase の読み書きや公開サイトの確認は GitHub Actions を踏み台にする**。
詳細と手順は `docs/OPERATIONS.md` を参照。

## データを扱うときの注意

このリポジトリは **public** で、**Actions のログと artifact も公開される**。
タスクには個人情報（確定申告・通院・冠婚葬祭など）が含まれるため、
**タスクの中身をログや artifact に出力しない**こと。
構造・件数・フィールド名までに留める（`scripts/inspect-tasks.mjs` がその実装例）。
